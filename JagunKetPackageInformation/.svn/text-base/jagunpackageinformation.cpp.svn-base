#include "jagunpackageinformation.h"

QString JagunPackageInformation::getLiteralTypePackage(int etp)
{
    switch(etp)
    {
        case(ETPNewConnection):
            return "Nova Conexao";
        case(ETPConnectionEstablished):
            return "Conexao Estabelecida";
        case(ETPData):
            return "Dado";
        case(ETPConnectionClosed):
            return "Conexao Fechada";
        case(ETPNewData):
            return "Novo Dado";
        case(ETPEndData):
            return "Fim Dado";
        case(ETPConfirmationData):
            return "Confirmacao Dado";
        default:
            return "Tipo Desconhecido";
    }
}
